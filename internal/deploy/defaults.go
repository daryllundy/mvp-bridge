package deploy

const (
	// DefaultBuildCommand is the fallback build command used when detection is empty.
	DefaultBuildCommand = "npm run build"
	// DefaultOutputDir is the fallback build output directory used when detection is empty.
	DefaultOutputDir = "dist"
)
